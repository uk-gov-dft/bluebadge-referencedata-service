apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "referencedata-service.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "referencedata-service.name" . }}
    helm.sh/chart: {{ include "referencedata-service.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "referencedata-service.fullname" . }}:{{ .Values.adminservice.port }}{{ .Values.adminservice.path }}']
  restartPolicy: Never
